<% if notice %>
  <aside id="notice"><%= notice %></aside>
<% end %>

<h2>Shopping Cart</h2>
<ul>
  <% @cart.line_items.each do |item| %>
    <li class="line-item"><%= item.quantity %>&times;<%= item.product.title %></li>
  <% end %>
</ul>

<%= button_to 'Empty cart', @cart, method: :delete, data: {confirm: 'Are you sure?'} %>
